# .github/

Placeholder for repository workflow files (issue templates, pull request
template, CI). CI pipelines are explicitly out of scope for PM-0001 and
must be authorized by a later directive before being added.
