# wordpress-plugin/

Placeholder. A companion WordPress bridge/plugin is built only if the
Phase 0.4 PowerPress proof of concept shows the standard WordPress REST
API is insufficient for enclosure management (see `pm/DECISIONS.md`
D-004 and `pm/ROADMAP.md`). Intentionally empty until then.
